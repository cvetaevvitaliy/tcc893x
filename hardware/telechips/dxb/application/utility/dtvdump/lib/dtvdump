# Script to start "dxbdump" on the device, which has a very rudimentary
# shell.
#
base=/system
export CLASSPATH=$base/framework/dtvdump.jar
exec app_process $base/bin com.telechips.android.dtvdump.DtvDump "$@"

